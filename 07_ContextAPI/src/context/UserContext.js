import React from "react";
//you need to create a context and also provider is compulsory along with context
//context is nothing but a global variable with global access.
const UserContext = React.createContext()


export default UserContext;